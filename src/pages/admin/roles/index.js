import React from 'react';
import { Container, Typography } from '@mui/material';

export default function AdminRolesPage() {
  return (
    <Container>
      <Typography variant="h4" sx={{ mt: 4 }}>Admin: Roles</Typography>
      <Typography>Role management content goes here.</Typography>
    </Container>
  );
}